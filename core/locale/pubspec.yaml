name: app_locale
description: "Locale package for flutter playground"
version: 1.0.0
publish_to: "none"
resolution: workspace

environment:
  sdk: ^3.10.4

dependencies:
  flutter_localizations:
    sdk: flutter
  flutter:
    sdk: flutter

  flutter_bloc: ^9.1.1
  intl: ^0.20.2
  common: ^0.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  intl_utils: ^2.8.14
  build_runner: ^2.13.1

flutter:
  generate: true